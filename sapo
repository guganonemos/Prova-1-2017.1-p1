#include <stdio.h>
int main()
{
	int n,m,o,casa,v;
	int i,j,k;
	scanf("%d%d%d", &n, &m, &o);
	int tabuleiro[n], jogadores[m];
	for(i=0;i<n;i++)
	{
		scanf("%d",&casa);
		tabuleiro[i] = casa;

	}
	for(i=0;i<o;i++)
	{
		for(j=0;j<m;j++)
		{
			scanf("%d",&v);
			
		}
		j=0;
	}
	return 0;
}
